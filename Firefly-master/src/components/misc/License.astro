---
import { licenseConfig } from "@/config/licenseConfig";
import { profileConfig } from "@/config/profileConfig";
import { siteConfig } from "@/config/siteConfig";
import I18nKey from "@/i18n/i18nKey";
import { i18n } from "@/i18n/translation";
import { formatDateToYYYYMMDD } from "@/utils/date-utils";
import watermarkLogo from "@/assets/images/firefly.png";

interface Props {
	title: string;
	id: string;
	pubDate: Date;
	class: string;
	author: string;
	sourceLink: string;
	licenseName: string;
	licenseUrl: string;
}

const { title, pubDate, author, sourceLink, licenseName, licenseUrl } =
	Astro.props;
const className = Astro.props.class;
const profileConf = profileConfig;
const licenseConf = licenseConfig;
const postUrl = sourceLink || decodeURIComponent(Astro.url.toString());
---

<div
  class={`relative transition overflow-hidden bg-(--license-block-bg) py-5 px-6 ${className}`}
>
  <div class="transition font-bold text-black/75 dark:text-white/75">
    {title}
  </div>
  <a href={postUrl} class="link text-(--primary) break-all">
    {postUrl}
  </a>
  <div class="flex flex-col sm:flex-row gap-3 sm:gap-6 mt-2">
    <div>
      <div class="transition text-black/30 dark:text-white/30 text-sm">
        {i18n(I18nKey.author)}
      </div>
      <div class="transition text-black/75 dark:text-white/75 line-clamp-2">
        {author || profileConf.name}
      </div>
    </div>
    <div>
      <div class="transition text-black/30 dark:text-white/30 text-sm">
        {i18n(I18nKey.publishedAt)}
      </div>
      <div class="transition text-black/75 dark:text-white/75 line-clamp-2">
        {formatDateToYYYYMMDD(pubDate)}
      </div>
    </div>
    <div>
      <div class="transition text-black/30 dark:text-white/30 text-sm">
        {i18n(I18nKey.license)}
      </div>
      <a
        href={licenseName ? licenseUrl || undefined : licenseConf.url}
        target="_blank"
        class="link text-(--primary) line-clamp-2"
        >{licenseName || licenseConf.name}</a
      >
    </div>
  </div>
  <div class="mt-4 pt-4 border-t border-black/5 dark:border-white/10 text-xs leading-6 text-black/40 dark:text-white/40">
    本文首发于「{siteConfig.title}」，作者：山风（余涛）。
    <span class="font-medium text-black/60 dark:text-white/60">欢迎转发、分享本文链接</span>，
    但
    <span class="font-medium text-black/60 dark:text-white/60">禁止任何形式的未授权转载、摘编、改写或商业使用</span>。
  </div>
  <img
    src={watermarkLogo.src}
    alt=""
    aria-hidden="true"
    class="absolute pointer-events-none right-8 top-6 h-[9rem] w-[9rem] object-contain opacity-[0.14] grayscale contrast-0 brightness-125 dark:opacity-[0.1]"
  />
</div>
